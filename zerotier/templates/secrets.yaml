apiVersion: v1
kind: Secret
metadata:
  name: {{ template "zerotier.fullname" . }}
  labels:
    app: {{ template "zerotier.fullname" . }}
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
type: Opaque
data:
  zt_api_key: "{{ .Values.zt_api_key }}"
